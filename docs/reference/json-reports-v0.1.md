# JSON Reports

This page documents the JSON reports currently produced by OrbitFabric.

Current documented baseline:

```text
v0.5.0 — Commandability and Autonomy Contracts
```

OrbitFabric JSON reports are generated artifacts intended for:

- CI inspection;
- automated validation workflows;
- reproducible scenario evidence;
- future integration tooling.

They are not the source of truth.

The source of truth remains the Mission Model YAML and scenario YAML.

---

## Stability status

The JSON report structures documented here are development-preview contracts.

They are stable enough for current v0.5 workflows, but they are not a v1.0 compatibility promise.

Future versions may introduce:

- explicit report schema versions;
- JSON Schema definitions;
- additional metadata fields;
- richer diagnostic structures;
- compatibility checks between tool version and Mission Model version.

---

## Version fields

OrbitFabric JSON reports include the OrbitFabric tool/package version in the top-level `version` field.

Current example:

```json
{
  "tool": "orbitfabric-lint",
  "version": "0.5.0"
}
```

This identifies the OrbitFabric tool version that produced the report.

Mission Model version is represented separately.

For lint reports, the Mission Model version is currently exposed as:

```json
{
  "mission": "demo-3u",
  "model_version": "0.1.0"
}
```

The top-level `version` and `model_version` fields have different meanings.

See also:

```text
Reference -> Versioning Model
```

---

## Lint report

Generated by:

```bash
orbitfabric lint examples/demo-3u/mission/   --json generated/reports/lint_report.json
```

Tool identifier:

```text
orbitfabric-lint
```

---

## Lint report top-level fields

| Field | Type | Meaning |
|---|---:|---|
| `tool` | string | Report producer. Current value: `orbitfabric-lint`. |
| `version` | string | OrbitFabric tool/package version that produced the report. |
| `mission` | string | Mission ID loaded from the Mission Model. |
| `model_version` | string | Mission Model version declared by the mission. |
| `result` | string | Machine-readable lint result. |
| `loaded` | object | Counts of loaded core Mission Model domains. |
| `summary` | object | Count of findings by severity. |
| `findings` | array | List of lint findings. |

---

## Lint result values

Current lint report `result` values:

| Value | Meaning |
|---|---|
| `passed` | No error or warning findings. |
| `passed_with_warnings` | Warning findings exist, but no error findings exist. |
| `failed` | At least one error finding exists. |

---

## Lint `loaded` object

The `loaded` object reports how many core Mission Model objects were loaded.

Current fields:

| Field | Meaning |
|---|---|
| `spacecraft` | Number of spacecraft records. Currently always `1` for a valid mission. |
| `subsystems` | Number of subsystem definitions. |
| `modes` | Number of operational modes. |
| `mode_transitions` | Number of mode transitions. |
| `telemetry` | Number of telemetry items. |
| `commands` | Number of command definitions. |
| `events` | Number of event definitions. |
| `faults` | Number of fault definitions. |
| `packets` | Number of packet definitions. |

Note: optional domains such as `payloads`, `data_products` and `contacts` are validated and documented, but they are not currently included in the lint JSON `loaded` object. This may be expanded in a future report schema revision.

---

## Finding shape

Each finding uses this shape:

```json
{
  "severity": "WARNING",
  "code": "OF-DL-004",
  "file": "data_products.yaml",
  "domain": "data_products",
  "object_id": "payload.radiation_histogram",
  "message": "high-priority data product has downlink intent but is not eligible in any downlink flow",
  "suggestion": "Add the data product to a downlink flow eligible_data_products list, or revise its downlink intent."
}
```

---

## Lint report example

Compact example:

```json
{
  "tool": "orbitfabric-lint",
  "version": "0.5.0",
  "mission": "demo-3u",
  "model_version": "0.1.0",
  "result": "passed",
  "loaded": {
    "spacecraft": 1,
    "subsystems": 4,
    "modes": 6,
    "mode_transitions": 6,
    "telemetry": 5,
    "commands": 4,
    "events": 8,
    "faults": 2,
    "packets": 3
  },
  "summary": {
    "errors": 0,
    "warnings": 0,
    "info": 0
  },
  "findings": []
}
```

---

## Simulation report

Generated by:

```bash
orbitfabric sim examples/demo-3u/scenarios/battery_low_during_payload.yaml   --json generated/reports/battery_low_during_payload_report.json
```

Tool identifier:

```text
orbitfabric-sim
```

---

## Simulation report top-level fields

| Field | Type | Meaning |
|---|---:|---|
| `tool` | string | Report producer. Current value: `orbitfabric-sim`. |
| `version` | string | OrbitFabric tool/package version that produced the report. |
| `mission` | string | Mission ID. |
| `scenario` | string | Scenario ID. |
| `result` | string | Scenario execution result. |
| `summary` | object | Counts of key execution outputs. |
| `timeline` | array | Ordered timeline entries. |
| `events` | array | Emitted events. |
| `commands` | array | Executed commands. |
| `mode_transitions` | array | Mode transitions. |
| `final_state` | object | Final simulator state. |
| `failed_expectations` | array | Failed scenario expectations. |

---

## Simulation result values

Current simulation report `result` values:

| Value | Meaning |
|---|---|
| `passed` | All scenario expectations passed. |
| `failed` | One or more scenario expectations failed. |

---

## Simulation summary object

Current fields:

| Field | Meaning |
|---|---|
| `events` | Number of emitted events. |
| `commands` | Number of executed or auto-dispatched commands. |
| `mode_transitions` | Number of mode transitions. |
| `failed_expectations` | Number of failed expectations. |

---

## Final state example

Example:

```json
{
  "mode": "DEGRADED",
  "telemetry": {
    "obc.mode": "NOMINAL",
    "eps.battery.voltage": 6.7,
    "eps.battery.current": 0.4,
    "payload.acquisition.active": false,
    "radio.downlink.available": true
  }
}
```

---

## Simulation report example

Compact example:

```json
{
  "tool": "orbitfabric-sim",
  "version": "0.5.0",
  "mission": "demo-3u",
  "scenario": "battery_low_during_payload",
  "result": "passed",
  "summary": {
    "events": 3,
    "commands": 2,
    "mode_transitions": 2,
    "failed_expectations": 0
  },
  "timeline": [],
  "events": [],
  "commands": [],
  "mode_transitions": [],
  "final_state": {
    "mode": "DEGRADED",
    "telemetry": {}
  },
  "failed_expectations": []
}
```

---

## Current limitations

Current development-preview reports do not yet include:

- explicit report schema version;
- JSON Schema URL;
- report generation timestamp;
- source file paths;
- Git commit SHA;
- environment metadata;
- compatibility matrix;
- stable v1.0 compatibility guarantee;
- optional payload/data product/contact loaded counts in the lint report.
